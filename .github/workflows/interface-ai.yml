name: interface-ai

# One workflow per assessment folder, triggered only by changes to that folder.
on:
  push:
    branches: [main]
    paths: ["interface-ai/**", ".github/workflows/interface-ai.yml"]
  pull_request:
    paths: ["interface-ai/**", ".github/workflows/interface-ai.yml"]

concurrency:
  group: interface-ai-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  UV_FROZEN: "1"  # fail if uv.lock is out of date with pyproject.toml

defaults:
  run:
    working-directory: interface-ai

jobs:
  checks:
    name: Lint, typecheck, test
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
      - uses: astral-sh/setup-uv@v6
        with:
          python-version: "3.12"
          enable-cache: true
          cache-dependency-glob: interface-ai/uv.lock
      - run: uv sync
      - name: Lint
        run: make lint
      - name: Typecheck
        run: make typecheck
      - name: Unit + integration tests
        run: make test

  e2e:
    name: E2E (Playwright)
    needs: checks
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5
      - uses: astral-sh/setup-uv@v6
        with:
          python-version: "3.12"
          enable-cache: true
          cache-dependency-glob: interface-ai/uv.lock
      - run: uv sync
      - name: Cache Playwright browsers
        id: pw-cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('interface-ai/uv.lock') }}
      - name: Install Chromium
        if: steps.pw-cache.outputs.cache-hit != 'true'
        run: uv run playwright install --with-deps chromium
      - name: Install Chromium system deps
        if: steps.pw-cache.outputs.cache-hit == 'true'
        run: uv run playwright install-deps chromium
      - run: make test-e2e

  security:
    name: Secrets + dependency audit
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0  # scan full history for leaked secrets
      - uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - uses: astral-sh/setup-uv@v6
        with:
          python-version: "3.12"
          enable-cache: true
          cache-dependency-glob: interface-ai/uv.lock
      - run: uv sync
      - run: make audit

  docker:
    name: Docker build
    needs: checks
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5
      - uses: docker/setup-buildx-action@v3
      - uses: docker/build-push-action@v6
        with:
          context: interface-ai
          push: false
          tags: interface-ai:ci
          cache-from: type=gha,scope=interface-ai
          cache-to: type=gha,scope=interface-ai,mode=max
